import * as React from 'react';
import {shallow} from 'enzyme';

describe('Specs', () => {
  const setup = () => {
    return {
      
    };
  };
  
  it('FooBar', () => {
    expect(1).toEqual(1);
  });
});
