{
  "name": "react-video-snapshot",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "git@github.com:zzarcon/react-video-snapshot.git",
  "author": "Hector Leon Zarco Garcia <hezarco@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "ts-react-toolbox": "^0.0.17"
  },
  "engines": {
    "node": "^8.5.0"
  },
  "scripts": {
    "start": "webpack -w",
    "dev": "ts-react-toolbox dev",
    "test": "ts-react-toolbox test",
    "test:ci": "jest --runInBand --coverage",
    "build": "NODE_ENV=production tsc -p ./tsconfig.prod.json",
    "release": "npm version patch && git push --tags && git push && npm publish",
    "prepublishOnly": "yarn test:ci && yarn build"
  },
  "peerDependencies": {
    "react": "^16.2.0"
  }
}